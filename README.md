# template-bun-hono-sqlite

This is a template for a simple app.

## Usage

To install dependencies:

```bash
bun install
```

To run the app in development mode, use the following command:
```bash
bun run dev
```
